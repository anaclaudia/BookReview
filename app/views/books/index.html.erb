<h1 class= "text-center">Books</h1>
<% @books.each do |book| %>
	<h2><%= link_to book.title, book_path(book), class: "btn btn-link" %><h2>
<% end %>

<div class="col-sm-2">
	<%= link_to "Add Book", new_book_path, class: "btn btn-xl btn-primary" %>
</div>
