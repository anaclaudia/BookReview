<h1 class= "text-center">Books</h1>
<ul>
	<% @books.each do |book| %>
		<li><%= link_to "#{book.title}", book_path(book), class: "btn btn-link" %></li>
	<% end %>
</ul>

<div class="col-sm-2">
	<%= link_to "Add Book", new_book_path, class: "btn btn-xl btn-primary" %>
</div>
